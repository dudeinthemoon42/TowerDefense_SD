#ifndef _MONEYMANAGER_
#define _MONEYMANAGER_

#include "Tower.h"
#include "Enemy.h"
#include <string>

using namespace std;



class MoneyManager{

public:
	static int totalMoney; 
	static int rodPrice;
	static int gunPrice;
	static int netPrice;
	static int bpPrice;
	static int lightningPrice;
	static int octopusPrice;
	static int goldfishPrice;
	static int sharkPrice;
	static int tritonPrice;
	static int seahorsePrice;
	static int totalPoints;

	static bool hasMoney(Tower *); //Does the user have enough money for the tower?
	static void takeMoney(Tower *); //Does the user have enough money for the tower?
	static void addMoney(Enemy *);
	static void addMoney(int); //Adds the int parameter to the totalMoney.
	
};

#endif // _MONEYMANAGER_
